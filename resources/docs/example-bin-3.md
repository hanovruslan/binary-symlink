```
    "extra": {
        "evolaze-binary-symlink": {
            "links" => {
                {
                    "from": "path/to/some_bin",
                    "to": "bin"
                }
            }
        }
    }

```

Create symlink from `app/path/to/some_bin` to `bin/bin`
